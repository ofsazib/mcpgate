"""Pytest plugin: `mcp_server` fixture and `tool_contract` helper (v0.1.0)."""

from __future__ import annotations

import pytest


@pytest.fixture
def mcp_server() -> object:  # pragma: no cover - replaced in Phase 5
    raise NotImplementedError("mcp_server fixture lands in mcpgate 0.1.0 Phase 5")
